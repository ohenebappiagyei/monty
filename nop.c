#include "monty.h"
/**
 * nop - Does nothing.
 * @stack: Double pointer to the to the beginning of the stack.
 * @line_number: The line number of the opcode being executed.
*/
void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack; /* Avoid unused variable warning */
	(void) line_number; /* Avoid unused variable warning */
}
